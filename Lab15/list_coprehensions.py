# TASK (filter list): crete new list from input, where elements are > 3
# input_data = [2,3,4,2,6,5]


# Variant1 - NOT PYTHONIC
# filterd_data = []
# for num in input_data:
#     if num>3:
#         filterd_data.append(num)

# Variant2 - (list comprehension) PYTHONIC
# filterd_data = [num for num in input_data if num>3]
# print(filterd_data)


# TASK (map list): map input_data to upper_letter_data  = ['A', 'B', 'C']
input_data = ['a', 'b', 'c']


### Variant 1 - NOT PYTHONIC
# upper_letter_data = []
# for l in input_data:
#     upper_letter_data.append( l.upper() )

### Variant2 - (list comprehension) PYTHONIC
# upper_letter_data = [l.upper() for l in input_data]



